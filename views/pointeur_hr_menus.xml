<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Menu racine -->
        <menuitem id="pointeur_hr_menu_root"
                  name="Pointeur HR"
                  sequence="80"/>

        <!-- Sous-menu Présences -->
        <menuitem id="pointeur_hr_menu_attendance"
                  name="Présences"
                  parent="pointeur_hr_menu_root"
                  sequence="10"/>

        <!-- Menu Pointeur sous le menu des présences -->
        <menuitem id="pointeur_hr_menu_import_root"
                  name="Pointeur"
                  parent="hr_attendance.menu_hr_attendance_root"
                  sequence="20"/>

        <!-- Ajout de l'import des données du pointeur -->
        <menuitem id="pointeur_hr_menu_import"
                  name="Import du pointeur"
                  parent="pointeur_hr_menu_import_root"
                  action="pointeur_hr_action_import"
                  sequence="10"/>

        <!-- Ajout des lignes d'import -->
        <menuitem id="pointeur_hr_menu_import_line"
                  name="Lignes d'import"
                  parent="pointeur_hr_menu_import_root"
                  action="pointeur_hr_action_import_line"
                  sequence="20"/>

        <!-- Menu Reporting des présences -->
        <menuitem id="pointeur_hr_menu_reporting"
                  name="Reporting"
                  parent="hr_attendance.menu_hr_attendance_root"
                  sequence="30"/>

        <!-- Ajout du rapport dans le menu Reporting -->
        <menuitem id="pointeur_hr_menu_attendance_report"
                  name="Rapport de présence"
                  parent="pointeur_hr_menu_reporting"
                  action="pointeur_hr_action_hr_attendance_report"
                  sequence="10"/>

        <!-- Menu Configuration -->
        <menuitem id="menu_pointeur_hr_config"
                  name="Configuration Pointeur"
                  parent="hr_attendance.menu_hr_attendance_settings"
                  sequence="100"/>

        <!-- Menu Lieux de pointage -->
        <menuitem id="pointeur_hr_menu_location"
                  name="Lieux de pointage"
                  parent="menu_pointeur_hr_config"
                  action="pointeur_hr_action_location"
                  sequence="10"/>

        <!-- Menu Correspondances employés -->
        <menuitem id="menu_pointeur_hr_employee_mapping"
                  name="Correspondances employés"
                  parent="menu_pointeur_hr_config"
                  action="action_pointeur_hr_employee_mapping"
                  sequence="20"/>
    </data>
</odoo>
